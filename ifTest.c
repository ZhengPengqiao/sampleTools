#include <stdio.h>

int main()
{
	int a;
	a = -1;
	if(a)
	{
		printf("if语句中 %d 是 TRUE\n", a);
	}
	else
	{
		printf("if语句中 %d 是 FALSE\n", a);
	}
	
	a = 0;
	if(a)
	{
		printf("if语句中 %d 是 TRUE\n", a);
	}
	else
	{
		printf("if语句中 %d 是 FALSE\n", a);
	}
	
	a = 1;
	if(a)
	{
		printf("if语句中 %d 是 TRUE\n", a);
	}
	else
	{
		printf("if语句中 %d 是 FALSE\n", a);
	}
	
	a = 2;
	if(a)
	{
		printf("if语句中 %d 是 TRUE\n", a);
	}
	else
	{
		printf("if语句中 %d 是 FALSE\n", a);
	}
	return 0;
}
